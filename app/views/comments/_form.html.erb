<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :commerter %><br>
    <%= f.text_field :commerter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
